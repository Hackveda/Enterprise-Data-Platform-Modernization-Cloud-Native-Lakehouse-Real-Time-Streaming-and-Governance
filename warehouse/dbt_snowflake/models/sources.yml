version: 2

sources:
  - name: raw
    database: "{{ env_var('SNOWFLAKE_DATABASE', 'ENTERPRISE_PLATFORM') }}"
    schema: RAW
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_order_events

models:
  - name: stg_customers
    columns:
      - name: customer_id
        tests: [not_null, unique]
  - name: stg_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
      - name: customer_id
        tests: [not_null]
  - name: stg_order_events
    columns:
      - name: event_id
        tests: [not_null, unique]
      - name: order_id
        tests: [not_null]
      - name: customer_id
        tests: [not_null]
  - name: mart_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
      - name: customer_id
        tests: [not_null]
  - name: mart_order_events
    columns:
      - name: event_id
        tests: [not_null, unique]
